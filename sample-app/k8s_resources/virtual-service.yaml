apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: sample-app-v1-vs
  namespace: dev
spec:
  hosts: 
  - "*"
  gateways:
  - sample-app-gw
  http:
    - route:
      - destination: 
          host: sample-app